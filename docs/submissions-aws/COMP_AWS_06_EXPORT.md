# COMP-AWS-06 public-export notes

This tree **is** the sanitized Apache-2.0 public competition export of private-main checkpoint `387250683e514815ad3d1aa122939f94c56b57c7`.

See [BUILD_PROVENANCE.md](../../BUILD_PROVENANCE.md).

It was rebuilt from `git archive` of that SHA (not from a stale sandbox snapshot and not from a former feature-branch SHA). Operator IAM JSON, Builder ID details, `.claude/`, `.env` values, account ids, AWS CLI profile names, and local absolute paths were omitted or rewritten.

LiteAPI and Prioticket appear as adapters with configuration **names** only. Parking remains a labelled simulation. Rental is requirement-only.
