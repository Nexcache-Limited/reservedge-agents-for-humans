# COMP-AWS-06 public-export notes

This tree **is** the sanitized Apache-2.0 public competition export of private-main checkpoint `0e24c62b203dd046125acf313a08dc78b77ba14d`.

See [BUILD_PROVENANCE.md](../../BUILD_PROVENANCE.md).

It was rebuilt from `git archive` of that SHA (not from a stale sandbox snapshot and not from a former feature-branch SHA). Operator IAM JSON, Builder ID details, `.claude/`, `.env` values, account ids, and local absolute paths were omitted or rewritten.

Do not `git push` a public remote, open the public GitHub repository, or submit Devpost from an agent. Product Owner review is required before publish.
