# docs

Project documentation.

| Path               | Role                                                                               |
| ------------------ | ---------------------------------------------------------------------------------- |
| `source-of-truth/` | Approved specification documents                                                   |
| `adr/`             | Architecture decision records                                                      |
| `work-orders/`     | Issued Cursor work orders (execution records)                                      |
| `threat-model/`    | Threat model (later)                                                               |
| `runbooks/`        | Operator runbooks (later)                                                          |
| `submissions/`     | Earlier competition drafts                                                         |
| `submissions-aws/` | AWS Agents for Humans pack (COMP-AWS-05); not published                            |
| `demo-aws/`        | Demo script, live recording shot list (gated), architecture, AgentCore design-only |

Normative priority lives in `source-of-truth/README.md`. Work orders do not outrank the approved specifications.
