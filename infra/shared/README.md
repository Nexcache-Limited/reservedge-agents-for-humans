# infra/shared

Shared infrastructure definitions that remain provider-neutral where possible.

WP-01 includes only local Docker Compose for PostgreSQL at the repository root. Production infrastructure is out of scope.
