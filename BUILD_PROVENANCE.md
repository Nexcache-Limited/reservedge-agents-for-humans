# Public-export build provenance

This tree is a sanitized Apache-2.0 public competition export. It is not the
private product repository.

| Item                               | Value                                                                       |
| ---------------------------------- | --------------------------------------------------------------------------- |
| Source checkpoint (private `main`) | `0e24c62b203dd046125acf313a08dc78b77ba14d`                                  |
| Source commit subject              | `feat(comp-aws): conversation-first Strands/Bedrock agent checkpoint (#15)` |
| Export generation                  | `git archive` of that SHA, then public-export transforms in this sandbox    |
| License in this export             | Apache License 2.0 (`LICENSE` + `NOTICE`)                                   |
| Private repository license         | Unchanged (not part of this export)                                         |

The SHA above identifies the accepted private-main competition baseline. This
file does not record local filesystem paths, operator names, AWS account
identifiers, IAM ARNs, or Builder ID details.

Amazon Bedrock AgentCore is not deployed in this checkpoint. Supplier execution
and payment remain simulated. This export is prepared for Product Owner review
and is not published by the export step itself.
