# Public-export build provenance

This tree is a sanitized Apache-2.0 public competition export. It is not the
private product repository.

| Item                               | Value                                                    |
| ---------------------------------- | -------------------------------------------------------- |
| Source checkpoint (private `main`) | `c79d0a667b1605cf4cfbe338e7a0509ec0ffd867` |
| Source commit subject              | `fix(comp): recording-final orchestration and UX corrections (#19)` |
| Export generation                  | `git archive` of that SHA, then public-export transforms |
| License in this export             | Apache License 2.0 (`LICENSE` + `NOTICE`)              |
| Private repository license         | Unchanged (not part of this export)                      |

The SHA above identifies the accepted private-main competition baseline. This
file does not record local filesystem paths, operator names, AWS account
identifiers, IAM ARNs, or Builder ID details.

Amazon Bedrock AgentCore is not deployed in this checkpoint. LiteAPI stay and
flight results are research only and wait for conversational authorization.
Prioticket city catalog inventory is currently provider-limited. Parking supplier
execution and payment remain simulated. Rental is requirement-only.
