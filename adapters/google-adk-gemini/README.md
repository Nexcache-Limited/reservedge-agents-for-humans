# adapters/google-adk-gemini

Google ADK, Gemini, and Google Cloud adapter surface for G1.

WP-01 forbids adding those SDKs. This directory exists so provider code has an approved home later. It must not leak into `packages/domain` or `packages/application`.
