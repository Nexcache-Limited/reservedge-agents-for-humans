# adapters/local

Local development adapters (in-process queues, local research stubs, PostgreSQL repository adapters later).

WP-01 does not implement adapters. Core packages must not import this tree.
