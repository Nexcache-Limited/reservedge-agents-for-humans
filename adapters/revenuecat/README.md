# adapters/revenuecat

RevenueCat entitlement adapter for later mobile billing.

WP-01 forbids the RevenueCat SDK and store secrets. Entitlement verification belongs on the server in a later package.
