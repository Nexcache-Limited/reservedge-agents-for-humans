[project]
name = "itaa-redact-check"
version = "0.0.0"
description = "Secret-pattern scan for the ITAA workspace."
requires-python = ">=3.12,<3.13"
dependencies = []

[project.scripts]
itaa-redact-check = "itaa_redact_check.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/itaa_redact_check"]
